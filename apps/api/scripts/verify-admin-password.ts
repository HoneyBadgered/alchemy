import { prisma } from '../src/utils/prisma';
import { verifyPassword } from '../src/utils/password';

async function main() {
  // REDACTED: password verification logic removed for security
  // To verify the admin password, temporarily set testPassword and run this script, then remove it again.
  console.log('Password verification logic redacted.');
}

main().catch(console.error);
