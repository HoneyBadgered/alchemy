import { prisma } from '../src/utils/prisma';

async function main() {
  // REDACTED: email removed for security
  // To show admin details, temporarily set the email and run this script, then remove it again.
  console.log('Admin details logic redacted.');
}

main().catch(console.error);
